$("#posts").append("<%= escape_javascript("#{ render @posts}").html_safe %>");

<% if @posts.next_page %>
    $("#post-pagination-link").attr('href', '<%= user_path(id: @user.id, posts_page: @posts.next_page) %>');
<% else %>
    $("#post-pagination-link").remove();
<% end %>