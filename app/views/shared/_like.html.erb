<%= form_for current_user.likes.build, html: {class: "float-right"} do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :post_id, value: post.id %>
  <%= f.submit "Like", class: "btn btn-outline-primary" %>
<% end %>