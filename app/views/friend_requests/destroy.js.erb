sweetAlert("Friend Request Canceled");
$("#friend_request-<%= @friend_request.receiver.id %>-links")
  .html("<%= escape_javascript(render('friend_requests/send_form', resource: @friend_request.receiver)) %>");
