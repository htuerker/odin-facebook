<% if @friendship.persisted? %>
  $("#friend_request-<%= @friendship.friend_id %>-links")
    .html("<%= escape_javascript(link_to 'Unfriend', @friendship, method: :delete, remote: true, class: 'btn btn-danger') %>");
<% end %>
